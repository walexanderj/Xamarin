<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Item.Nombre}"
             x:Class="CedeSistemasApp.Views.RestaurantDetailPage">
    <ContentPage.Content>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <Image Source="{Binding Item.Imagen}" Aspect="AspectFill" Grid.Row="0"/>
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Frame HasShadow="False" Margin="5" BorderColor="LightGray" CornerRadius="10" Grid.Row="1" BackgroundColor="Black" Opacity="80" >
                    <StackLayout  >
                        <Label Text="{Binding Item.Nombre}" FontSize="Subtitle" FontAttributes="Bold" TextColor="White"/>
                        <Label Text="{Binding Item.Direccion}" FontSize="Micro" TextColor="White"/>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Item.Telefono}"  FontSize="Small" TextColor="White"/>
                            <Label Text=" - "/>
                            <Label Text="{Binding Item.SitioWeb}" FontSize="Small" TextColor="White" />
                        </StackLayout>
                   </StackLayout>
                </Frame>

            </Grid>
            <Grid Grid.Row="1">
                <ListView x:Name="grd_products" 
                  IsRefreshing="{Binding IsRefreshing}"
                  ItemsSource="{Binding Productos}" 
                  HasUnevenRows="True" 
                  CachingStrategy="RetainElement" 
                  SeparatorVisibility="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid BackgroundColor="{StaticResource gris_color_claro}">
                                    <Frame HasShadow="False" Margin="5" BorderColor="LightGray" CornerRadius="10" >
                                        <StackLayout Orientation="Horizontal">
                                            <StackLayout Orientation="Vertical">
                                                <Label Text="{Binding Nombre}" FontSize="Subtitle" />
                                                <Label Text="{Binding Descripcion}" FontSize="Micro" TextColor="Gray"/>
                                                <Label Text="{Binding Precio}" FontSize="Small"/>
                                            </StackLayout>
                                        </StackLayout>
                                    </Frame>
                                </Grid>

                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>

    </ContentPage.Content>
</ContentPage>